(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.AmpereForceModel=api})(typeof self!=='undefined'?self:this,function(){const clamp=(v,a,b)=>Math.min(b,Math.max(a,Number(v)));function normalize(i={}){return{currentA:clamp(i.currentA??5,0,20),lengthM:clamp(i.lengthM??.4,.05,1),fieldT:clamp(i.fieldT??.5,0,2),angleDeg:clamp(i.angleDeg??90,0,180),massG:clamp(i.massG??20,1,200),currentSign:Number(i.currentSign)<0?-1:1,fieldSign:Number(i.fieldSign)<0?-1:1,gravity:clamp(i.gravity??9.8,1.6,12)}}function solve(i={}){const n=normalize(i),theta=n.angleDeg*Math.PI/180,signedForceN=-n.currentSign*n.fieldSign*n.fieldT*n.currentA*n.lengthM*Math.sin(theta),forceN=Math.abs(signedForceN),weightN=n.massG*1e-3*n.gravity,netVerticalN=signedForceN-weightN,balanceCurrentA=n.fieldT*n.lengthM*Math.abs(Math.sin(theta))<1e-12?Infinity:weightN/(n.fieldT*n.lengthM*Math.abs(Math.sin(theta))),wireUnit={x:Math.cos(theta),y:Math.sin(theta),z:0},fieldUnit={x:n.fieldSign,y:0,z:0};return{...n,theta,signedForceN,forceN,weightN,netVerticalN,balanceCurrentA,wireUnit,fieldUnit,forceUnit:{x:0,y:0,z:Math.sign(signedForceN)},crossResidualN:forceN-n.fieldT*n.currentA*n.lengthM*Math.abs(Math.sin(theta)),accelerationMs2:netVerticalN/(n.massG*1e-3)}}function angleSeries(i={},count=180){return Array.from({length:count+1},(_,k)=>solve({...i,angleDeg:180*k/count}))}return{normalize,solve,angleSeries}});

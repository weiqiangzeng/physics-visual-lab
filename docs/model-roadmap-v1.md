# Model Roadmap v1

当前课程共 59 个模型，全部标记为 `open`，并按知识关系映射到 39 个交互实验。多个紧密关联模型可以共享一页连续工作台，但只有页面实际具备核心场景、可调参数和验证证据时才复用映射。

模型筛选继续使用 `featured` 和 `status`：`featured` 控制学生目录密度，`open` 表示已有可访问实验。课程完整数据仍保留模型顺序，不以首页卡片顺序替代教材关系。

后续迭代优先处理使用反馈、可访问性和物理边界细化。新增模型仍按可观察现象、课程价值、参数可控性、模型边界和“预测—验证—解释”闭环筛选。

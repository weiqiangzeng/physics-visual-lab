# Model Roadmap v1

当前课程目录保留已有模型和精选目录。第一阶段不增加实验数量，只为已有模型增加版本标识和兼容协议。

模型筛选继续使用 `featured` 和 `status`：`open` 表示已有实验，`planned` 表示已有模型内容但尚未开放交互实验。

下一阶段才评估新增模型，筛选依据为：可观察现象、课程价值、参数可控性、模型边界是否清楚，以及是否能形成预测—验证—解释闭环。
